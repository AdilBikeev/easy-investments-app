version: '3.4'

services:
  stock.api:
    image: ${DOCKER_REGISTRY-}stockapi
    build:
      context: .
      dockerfile: Serivces/Stock/Stock.API/Dockerfile

  rabbitmq:
    image: rabbitmq:3-management-alpine

  postgres:
    image: postgres:13-alpine